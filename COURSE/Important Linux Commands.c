default programing lnguage for unix linux programming language.

linux is a unix-like os

commands:

pwd         => "print current (working) directory", the location you ae in right now

ls          => "Listing", see all files is a folder

cd ..       => "change directory" return to containing folder

ls -l       => "to list files and directories" gives more info

-something is called flag, passing flag to alter the output

ls -la      => lists everything, even hidden files


absolute path - from the very beginning
relative path ..
parent folder .