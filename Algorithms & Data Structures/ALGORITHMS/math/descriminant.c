#include <math.h>

int main()
{
    double a, b, c;            // inputs
    double disc, root1, root2; // outputs

    disc = pow(b, 2) - 4 * a * c;
    root1 = (-b + sqrt(disc)) / (2 * a);
    root2 = (-b - sqrt(disc)) / (2 * a);
}