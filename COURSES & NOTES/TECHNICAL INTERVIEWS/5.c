// what is the output?

void main()
{
    char ch = "A"; // chacater constant (65)
    ch = ch + 1;   // adding one to its ascii code (65)
    printf("%c", ch);
}

/*

chacater constant stored in the form the binary of its ASCII value
ASCI CODE OF A IS 65

1 character stored in 1 byte (8 bits)
output: 'B'

*/